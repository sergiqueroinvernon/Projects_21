@page
@model 
Web.Pages.Cesar.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CesarNum[0].Nummer)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CesarNum[0].Conversion)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.CesarNum) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nummer)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Conversion)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Bearbeiten</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Löschen</a>
            </td>
        </tr>
}
    </tbody>
</table>
